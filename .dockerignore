# Keep the Docker build context small and deterministic.
# NOTE: crates/explorer/wwwroot/explorer IS required (embedded into the binary),
# so it is intentionally NOT excluded.
.git
.gitignore
target/
**/target/
node_modules/
explorer/node_modules/
perf/
docs/
*.log
README.md
